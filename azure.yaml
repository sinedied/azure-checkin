# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-checkin
metadata:
  template: azure-checkin@1.0.0

services:
  webapp:
    project: ./
    dist: dist/azure-checkin
    language: ts
    host: staticwebapp

  api:
    project: ./api
    language: ts
    host: function
